// jQuery and Frameworks
$('#registerBtn').click(function () {
  alert("Registered via jQuery!");
});

$('.eventCard').fadeIn().fadeOut();

/* Benefit of frameworks like React/Vue:
   - Reactive state updates
   - Reusable components
   - Virtual DOM = faster UI */
